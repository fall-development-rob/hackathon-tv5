# Authentication Configuration (SPARC NFR-4.1)
# IMPORTANT: Never commit actual secrets to version control

# JWT Secrets - Generate with: openssl rand -base64 32
JWT_SECRET=your-secret-key-here
JWT_REFRESH_SECRET=your-refresh-secret-key-here

# Token Expiry (in seconds)
# ACCESS_TOKEN_EXPIRY=86400          # 24 hours (default)
# REFRESH_TOKEN_EXPIRY=2592000       # 30 days (default)

# Security Settings
# BCRYPT_COST=12                     # Cost factor (default)
# MAX_FAILED_ATTEMPTS=5              # Lockout threshold (default)
# LOCKOUT_DURATION_MINUTES=15        # Lockout duration (default)
